# Document parsing (Docling with RapidOCR - Python-only, no system packages)
docling>=2.0.0
rapidocr>=3.0.0

# PDF text extraction (for OCR detection)
pymupdf>=1.24.0

# Image handling (for test PDF generation)
pillow>=10.0.0

# Excel parsing (streaming, memory-safe)
openpyxl>=3.1.0

# AWS SDK
boto3>=1.34.0

# Graph Database (Neptune)
requests>=2.31.0
requests-aws4auth>=1.2.0

# Utilities
python-dotenv>=1.0.0
